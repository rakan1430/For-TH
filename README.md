# منصّة أ. ‹اسم المعلّم› — القدرات الكمّي والتحصيلي

منصّة تعليمية **مدفوعة** لمعلّم رياضيات واحد وطلّابه المشتركين: يرفع فيها
ملفّاته وروابطه واختباراته، ويوزّعها على مجموعاتٍ يقسّمها هو، ويقبل اشتراكات
الطلاب بنفسه.

> 🔴 **مشروعٌ جديد يبدأ من الصفر.** لا شفرة ولا قاعدة بيانات ولا حساب استضافة
> ولا نطاق ولا مستخدمين مشتركين مع أي مشروعٍ آخر للمالك. المنقول من المشروع
> السابق هو **الخبرة وحدها** — أنماطٌ وأخطاءٌ وقعت فعلاً، مجموعةٌ في
> `docs/المرجع/٢-خبرات-هندسية-منقولة.md`.

---

## الحالة الآن

| الطبقة | الحالة |
|---|---|
| قاعدة البيانات وحراستها | ✅ كاملة ومُختبَرة — ٩ مهاجرات، ١٦٩ تحقّقاً |
| الأمان والصلاحيات | ✅ قائمة بيضاء مكتوبة يدقّقها البناء |
| واجهة الطالب | ✅ الأقسام والبنوك والملفّات وأداء الاختبارات |
| **تأليف المحتوى** | ✅ الأقسام والبنوك، ورفع الملفّات والصور والروابط، والترتيب بالسحب، وبناء الاختبارات |
| المجموعات | ✅ إنشاء وتسمية ووسمٌ بلون وإدارة الأعضاء |
| التوزيع | ✅ إلى مجموعة، أو طلّاب بأسمائهم، أو كل مشتركي مسار |
| الاشتراك برفع الإيصال | ✅ من الطرفين — الرفع والطلب والقبول |
| قاعدة البيانات | ✅ موصولة — مشروع Supabase مستقلّ، ٩ مهاجرات مطبَّقة |
| الاستضافة | ✅ Netlify، تبني من `netlify.toml` عند كل دفعة |
| الدفع داخل المنصّة | ⏸️ ينتظر قرار المالك بالمزوّد (س-٣) |
| الأسعار | ⏸️ تظهر `[السعر]` — لم تُحدَّد (س-٢) |
| اسم المنصّة | ⏸️ عنصر نائب في مكانٍ واحد (س-١) |

> الأسئلة المفتوحة كلّها في `docs/سجل-المشروع.md`، القسم الأول.
> **لم يُخترع اسمٌ ولا سعرٌ ولا مزوّد دفع.**

---

## القسمان

المنصّة مقسومةٌ مسارين منفصلين، لكلٍّ **اشتراكه المستقلّ**:

| المسار | المحتوى |
|---|---|
| **القدرات — الكمّي** | شروحات القسم الكمّي. **الكمّي فقط** — لا لفظي |
| **التحصيلي — الرياضيات** | مادّة الرياضيات لاختبار التحصيلي |

والفصل بينهما **مفروضٌ في قاعدة البيانات لا في الواجهة**: طالبٌ مشترك في
التحصيلي وحده لا يقرأ صفّاً واحداً من محتوى القدرات ولو عبث بالطلب يدوياً.
وهذا مُختبَرٌ لا مُدّعىً — انظر `db/test/01_track_separation.test.sql`.

---

## بنية المستودع

```
supabase/migrations/   ٨ مهاجرات — البنية والحراسة والدوالّ والصلاحيات
db/
  local/               تهيئة قاعدة محلّية تشبه Supabase (للفحص وحده)
  test/                ١٣٨ تحقّقاً + خمسة أخطاء مقصودة تُحقن لإثبات الفحص
  security/            تدقيق الصلاحيات والقائمة البيضاء
app/                   الواجهة — Vite + React + TypeScript
scripts/               فحوص القياس والاتّفاقيات
docs/
  سجل-المشروع.md       ⭐ القرارات المُلزِمة، وما رُفض، والأخطاء التي وقعت
  الأمان.md            نموذج الحراسة والقائمة البيضاء
  المعمارية.md         كيف تتركّب القطع
  المرجع/              ملفّات المالك الأربع كما سُلّمت — لا تُعدَّل
```

---

## نسخة العرض — لرؤية الشكل قبل إنشاء القاعدة

بناءٌ منفصل ببياناتٍ تجريبية في المتصفّح وحده:

```bash
cd app && VITE_DEMO=1 npm run build && npx vite preview
```

وتنشره مهمّة `.github/workflows/preview.yml` على صفحات GitHub تلقائياً
(تحتاج تفعيلاً مرّةً واحدة: Settings ← Pages ← Source: GitHub Actions).

ولنشره صفحةً مستقلّة بمساراتٍ نسبية:

```bash
cd app && VITE_ARTIFACT=1 VITE_DEMO=1 npm run build
node ../scripts/build-artifact.mjs      # يُخرج app/dist/artifact.html
```

> ⚠️ **وضع العرض لا يحرس شيئاً**: لا سياسات صفوف ولا صلاحيات ولا فصل بين
> المسارين مفروضاً. كل ما يحرسه المشروع فعلاً يقع في قاعدة البيانات، وهي
> غائبة هناك. فما تراه يقول «هكذا يبدو الشكل» لا «هكذا يتصرّف النظام»،
> وشريطٌ في أعلى الصفحة يقول ذلك صراحةً.

ولأخذ لقطاتٍ في متصفّحٍ حقيقيّ (وهو ما كشف ثلاثة عيوبٍ لم يرها أي فحصٍ آخر):

```bash
cd app && npm i -D playwright
VITE_DEMO=1 npx vite build && npx vite preview --port 4174 &
node ../scripts/screenshots.mjs
```

---

## التشغيل

### فحوص قاعدة البيانات (لا تحتاج Supabase)

```bash
./db/test/run.sh              # قاعدة مؤقّتة تُنشأ وتُهدَم
./db/test/negative-control.sh # إثبات أنّ الفحوص ليست عمياء
```

يلزم Postgres مثبَّتاً محلّياً (أو `DATABASE_URL` لقاعدةٍ قائمة).

### الواجهة

```bash
cd app && npm install
cp .env.example .env          # ثم املأ القيم
npm run dev
```

بلا `.env` تعرض الواجهة شاشة إعدادٍ تشرح الخطوات — لا أخطاء شبكةٍ غامضة.

### كل الفحوص

```bash
node scripts/check-contrast.mjs --self-test     # التباين يُقاس لا يُقدَّر
cd app && npx tsc --noEmit && npx vitest run && npx vite build
node scripts/check-conventions.mjs --self-test
./db/test/run.sh && ./db/test/negative-control.sh
```

---

## الاستضافة

المستضيف المعتمد **Netlify**، مربوطٌ بالمستودع ويبني عند كل دفعة. والإعداد
كلّه في `netlify.toml` و`app/.env.production` — لا ضبط يدويّ في لوحته.

وصفحات GitHub احتياطٌ يدويّ (`.github/workflows/preview.yml`) يُشغَّل عند
الحاجة، ولا يعمل تلقائياً كي لا يبقى أحمر بلا سبب.

---

## ربط مشروع Supabase

1. أنشئ مشروعاً **جديداً ومستقلّاً** لهذه المنصّة وحدها.
2. طبّق `supabase/migrations/*.sql` بالترتيب.
3. سجّل حساب المعلّم من الواجهة، ثم أضفه مرّةً واحدة بـSQL:
   ```sql
   insert into private.teachers (user_id) values ('<معرّف المستخدم>');
   ```
   لا واجهة تفعل هذا ولا دالّة — **بعمد**: لا باب ترقيةٍ ذاتية في المشروع.
4. املأ `app/.env`.

---

## قواعد لا تُنقَض

هذه ليست تفضيلات. كلّ واحدةٍ منها خطأٌ وقع فعلاً وكلّف وقتاً.

1. **الحارس في الأسفل.** لا شرط صلاحيةٍ يُكتب في الواجهة بديلاً عن القاعدة.
2. **كل دالّة جديدة في `public`:** اسحب `EXECUTE` من `PUBLIC`، وامنحه لمن
   يحتاجه، وحدّث `db/security/expected-grants.txt`. وإلا أرسب التدقيق البناء.
3. **سمة العرض على `html` لا على `body`.**
4. **كل تاريخٍ يمرّ بـ`lib/format.ts`.** لا `toLocaleDateString` في مكانٍ آخر.
5. **لا `raise exception` بعد نجاح كتابة** — يُلغي المعاملة كلّها. أعد قيمة.
6. **لا نجاح كاذب.** ما لم يُكتب يُبلَّغ عنه بعددٍ صريح.
7. **فحصٌ جديد يحتاج إثباتاً أنّه يرسب** الخللَ الذي بُني لالتقاطه.
8. **حدِّث `docs/سجل-المشروع.md` مع كل قرار.** الذاكرة عبر الجلسات لا
   يُعتمد عليها.
