# تعليمات تشغيلٍ مُلزِمة — تُقرأ قبل أي عمل

> هذا الملفّ يُحمَّل تلقائياً في كل جلسة. ما فيه **قرارات المالك وحقائق
> البيئة**، لا اجتهاد. مخالفتها أهدرت جلساتٍ كاملة من قبل.

---

## ١) النشر على Netlify — **عبر Zapier وحده**

⚠️⚠️ **لا تستعمل اتصال Netlify المباشر (`mcp__Netlify__*`) لهذا المشروع
إطلاقاً.** ذلك الاتصال مربوطٌ بحسابٍ **آخر** (يملك `khutaa` و
`motaqadima-khuta` و`for-th-preview`). يرى مشروع `for-th` ولا يملك النشر
فيه — فيُجيب القراءةَ بنجاح ثمّ يردّ كل كتابةٍ بـ403 أو «this site and the
team it belongs to do not exist».

**الصحيح:** أدوات **Zapier** ← تطبيق **Netlify** (الحساب
`hrbmhcg@gmail.com`، فريق «teacher wep»). وفيها أفعالٌ جاهزة:

| الفعل | الغرض |
|---|---|
| `netlify_netlify_api_call` | أي نداءٍ موثَّق على واجهة Netlify |
| `netlify_start_deploy` | بدء نشرة |
| `netlify_netlify_deploy_chunked` | رفع ملفّاتٍ مبنيّة على دفعات |
| `netlify_netlify_verify_live` | التأكّد أنّ النشرة وصلت فعلاً |
| `netlify_http_probe` | جلب صفحةٍ بلا مصادقة |

**معرّف الموقع:** `81979a33-d642-4ded-9a70-27de1aabe1ad` — `for-th.netlify.app`

⚠️ **والاتصال المباشر قراءةٌ لا كتابة** — قيس لا تفترض: `deploy-site` فيه
يردّ **403 Forbidden** حتى على موقعٍ في فريقه هو. فكل كتابةٍ على Netlify
تمرّ بـZapier، بلا استثناء.

⚠️ **وحساب Netlify الثاني** (الذي فيه `khutaa` و`qalladha`) **ليس موصولاً
بـZapier**. للعمل عليه يُربط أوّلاً من `manage_zapier_connections`، ثمّ
يُمرَّر `connection_id` الخاصّ به في كل نداء. و**لا يُمسّ `khutaa` ولا
`qalladha` إطلاقاً** — قرار المالك.

⚠️ **والاقتصاد في الرصيد قرار مالك:** يُبنى المشروع **هنا** ثمّ تُرفع
الملفّات جاهزةً عبر واجهة النشر — فلا يُستهلك رصيد بناءٍ عندهم. ولا يُربط
الموقع البديل بـGitHub، لئلّا يبني مع كل دفعة.

⚠️ **ولا يكفي أن تدفع إلى GitHub ثمّ تقول «رُفع».** الدفع إلى المستودع
شيء، ووصولُه إلى الموقع الحيّ شيءٌ آخر. **تحقّق من النشرة بعد كل دفعة**
(`netlify_http_probe` على `/index.html` وقارن اسم الحزمة)، وإلّا أبلغتَ
المالك بنجاحٍ لم يقع.

## ٢) قاعدة البيانات — Supabase

المشروع: `tuqazxoedxrpyqqxaidt`. تُطبَّق المهاجرات عبر أدوات Supabase
(`mcp__Supabase__apply_migration`). ومفتاح الخدمة **لا يوضع في المتصفّح
ولا في متغيّرٍ يبدأ بـ`VITE_`** أبداً.

## ٣) الفرع

كل العمل على `claude/new-project-repositories-exeq9g` — وهو الفرع
الافتراضي للمستودع وفرع الإنتاج في Netlify. لا فرع `main`.

## ٤) الشبكة في بيئة التطوير

`netlify.app` و`supabase.co` ومنفذا 5432/6543 **محجوبةٌ بسياسة الخروج**.
فالقناة الوحيدة العاملة هي الموصلات (Zapier / Supabase MCP). لا تُفتِ في
الشبكة قبل أن تقيس (خ-١٣).

## ٥) خطوط حمراء

- **لا صلة بمنصّة «خُطى» ولا «ذكرى»**: لا قاعدة بيانات ولا حساب ولا شفرة.
  المنقول منها **الخبرة وحدها**.
- **دوران اثنان فقط**: معلّم وطالب. لا «إدارة» ولا «مشرف».
- **لا تخترع** اسم المعلّم ولا الأسعار ولا مزوّد دفع.
- نتائج الطلّاب لا يمحوها المعلّم. ويمحوها **صاحبها** بحذف حسابه (ق-١٠).

---

**والمرجع الكامل:** `docs/سجل-المشروع.md` — القرارات المُلزِمة والأخطاء
التي وقعت وكيف كُشفت. اقرأه قبل أي تغييرٍ في القاعدة أو الأمان.
