-- =============================================================================
-- تصفير القاعدة قبل كل تشغيل — للتطوير والفحص وحدهما
--
-- ⚠️ يجعل كل تشغيلٍ يبدأ من لا شيء، سواءٌ على قاعدةٍ مؤقّتة تُنشأ وتُهدَم أو
--    على خادمٍ قائم في التكامل المستمرّ. وفحصٌ يعتمد على بقايا تشغيلٍ سابق
--    ينجح مرّةً ويرسب أخرى بلا سببٍ ظاهر — وهذا أسوأ من رسوبٍ ثابت.
--
-- ⚠️⚠️ لا يُشغَّل على قاعدة إنتاجٍ أبداً: يحذف كل شيء.
-- =============================================================================
drop schema if exists public  cascade;
drop schema if exists private cascade;
drop schema if exists testing cascade;
drop schema if exists auth    cascade;
create schema public;
